{
  "name": "school-sport-day",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "start": "npm run build && STATIC_DIR=dist node server.mjs",
    "test": "node --test"
  },
  "dependencies": {
    "@supabase/server": "^1.6.1"
  }
}
